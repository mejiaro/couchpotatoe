= form_for @manage_user do |f|
  - if @manage_user.errors.any?
    #error_explanation
      %h2= "#{pluralize(@manage_user.errors.count, "error")} prohibited this manage_user from being saved:"
      %ul
        - @manage_user.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
